<!DOCTYPE html>
<html>
  <head>
    <title>CryptoWallet</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbolinks-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
      <div class="container-fluid">
        <ul class="nav justify-content-end">
          <li class="nav-item">
            <%= link_to 'HOME', root_path, class: "nav-link", class:"btn btn-link" %>
          </li>
          <li class="nav-item">
            <%= link_to 'Português', root_path(locale: 'pt-BR'), class: "nav-link", class:"btn btn-link" %>
          </li>
          <li class="nav-item">
            <%= link_to 'English', root_path(locale: 'en'), class: "nav-link", class:"btn btn-link" %>
          </li>
        </ul>        
        <%= yield %>
      </div>
    </body>
</html>